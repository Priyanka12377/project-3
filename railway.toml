[build]
  builder = "nixpacks"

[deploy]
  startCommand = "npm start"
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 10

[[services]]
name = "backend"
source = { type = "github", repo = "Priyanka12377/project-3", branch = "main" }
rootDir = "backend"

